# screc app icon — AI generator prompt

## Main prompt

App icon for "screc", a tiny, elegant macOS menu-bar utility that records the
screen (a window, a region, or the whole display) and instantly produces
small, share-ready MP4 clips and GIFs — the kind of polished little
one-purpose tool you'd find featured on the Mac App Store.

The icon is a stylized macOS application window: a single rounded rectangle
with a noticeably thicker top title bar. In the title bar, the three classic
macOS window-control buttons are stenciled out — punched clean through as
transparent/negative-space circles, not colored dots. Bottom-aligned inside
the window body sits the universally recognizable recording badge: a filled
red dot followed by the word "REC" in a bold, heavy, geometric sans-serif.

The red of the recording dot is the ONLY color in the entire icon — everything
else is strictly monochrome: soft near-white window body, light gray title
bar, dark charcoal outline and lettering. Subtle depth is welcome (a gentle
drop shadow under the window, a hint of vertical gradient on the surfaces),
but the overall look stays clean, flat-ish and contemporary.

Style: modern macOS Big Sur / Sonoma app-icon aesthetic — friendly rounded
geometry, crisp vector edges, balanced negative space, premium "indie Mac
utility" polish. Must read instantly at 16 px (red dot + window silhouette)
and reward a close look at 1024 px (the stenciled window controls).

Format: 1024×1024, centered composition on a transparent background, content
sized to the standard macOS icon grid (roughly 80–85 % of the canvas), no
background tile unless it is part of the window shape itself.

## Style keywords

macOS app icon, Big Sur style, squircle-era design, vector, flat with subtle
depth, soft shadow, monochrome + single red accent, minimal, geometric,
bold typography, screen recorder, REC badge, premium indie Mac utility

## Negative prompt

no additional colors, no rainbow gradients, no glossy 3D bevels, no
skeuomorphic camera lenses or film reels, no photorealism, no busy detail,
no text other than "REC", no colored traffic-light dots (they are punched-out
holes), no rounded-square background tile behind the window, no watermark
